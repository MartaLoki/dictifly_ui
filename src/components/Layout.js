import { faCediSign, faUpRightAndDownLeftFromCenter } from "@fortawesome/free-solid-svg-icons";
import { Outlet } from "react-router-dom";

const Layout = () =>  {
    return (
        <main>
            <Outlet/>

        </main>
    )
}
export default Layout